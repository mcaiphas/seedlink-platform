import { AdminListPage } from '@/components/AdminListPage';
import { Badge } from '@/components/ui/badge';
export default function SubcategoryList() { return <AdminListPage title="Subcategories" tableName="product_subcategories" selectQuery="*, product_categories(name)" searchPlaceholder="Search..." searchFields={['name','slug']} orderBy="sort_order" columns={[{label:'Name',key:'name',sortable:true,render:r=><span className="font-medium">{r.name}</span>},{label:'Category',key:'category_id',render:r=>(r.product_categories as any)?.name||'—'},{label:'Slug',key:'slug',render:r=><span className="font-mono text-xs text-muted-foreground">{r.slug}</span>},{label:'Status',key:'is_active',render:r=><Badge variant={r.is_active?'default':'secondary'}>{r.is_active?'Active':'Inactive'}</Badge>}]} />; }
