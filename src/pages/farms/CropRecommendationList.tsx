import { AdminListPage } from '@/components/AdminListPage';
import { Badge } from '@/components/ui/badge';
export default function CropRecommendationList() { return <AdminListPage title="Crop Recommendations" tableName="crop_recommendations" selectQuery="*, farms(farm_name)" searchPlaceholder="Search..." searchFields={['title','recommendation_type']} columns={[{label:'Title',key:'title',sortable:true,render:r=><span className="font-medium">{r.title}</span>},{label:'Type',key:'recommendation_type',render:r=><Badge variant="secondary" className="capitalize">{r.recommendation_type}</Badge>},{label:'Farm',key:'farm_id',render:r=>(r.farms as any)?.farm_name||'—'},{label:'Status',key:'status',sortable:true,render:r=><Badge variant={r.status==='accepted'?'default':'outline'} className="capitalize">{r.status}</Badge>},{label:'Date',key:'created_at',sortable:true,render:r=>new Date(r.created_at).toLocaleDateString()}]} />; }
