import { AdminListPage } from '@/components/AdminListPage';
import { Badge } from '@/components/ui/badge';
export default function DeliveryStatusList() { return <AdminListPage title="Delivery Status" tableName="delivery_status_logs" searchPlaceholder="Search logs..." searchFields={['status','notes']} columns={[{label:'Delivery',key:'delivery_request_id',render:r=><span className="font-mono text-xs">{r.delivery_request_id?.slice(0,8)||'—'}</span>},{label:'Status',key:'status',sortable:true,render:r=><Badge variant="secondary" className="capitalize">{r.status}</Badge>},{label:'Notes',key:'notes',render:r=><span className="text-sm text-muted-foreground truncate max-w-xs block">{r.notes||'—'}</span>},{label:'Timestamp',key:'created_at',sortable:true,render:r=>new Date(r.created_at).toLocaleString()}]} />; }
