import { PageHeader } from '@/components/PageHeader';
export default function YieldEstimatorList() {
  return <PageHeader title="Yield Estimator" description="Forecast expected crop yields based on field data — coming soon" />;
}
