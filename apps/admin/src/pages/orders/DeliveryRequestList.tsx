import { AdminListPage } from '@/components/AdminListPage';
import { Badge } from '@/components/ui/badge';
export default function DeliveryRequestList() { return <AdminListPage title="Delivery Requests" tableName="delivery_requests" searchPlaceholder="Search..." searchFields={['pickup_address','dropoff_address'] } columns={[{label:'ID',key:'id',render:r=><span className="font-mono text-xs">{r.id?.slice(0,8)}</span>},{label:'Pickup',key:'pickup_address',render:r=><span className="max-w-[160px] truncate block text-muted-foreground">{r.pickup_address}</span>},{label:'Dropoff',key:'dropoff_address',render:r=><span className="max-w-[160px] truncate block text-muted-foreground">{r.dropoff_address}</span>},{label:'Status',key:'delivery_status',sortable:true,render:r=><Badge variant="secondary">{r.delivery_status}</Badge>},{label:'Date',key:'created_at',sortable:true,render:r=>new Date(r.created_at).toLocaleDateString()}]} />; }
