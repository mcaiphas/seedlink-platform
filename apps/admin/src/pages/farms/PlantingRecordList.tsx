import { AdminListPage } from '@/components/AdminListPage';
import { Badge } from '@/components/ui/badge';
export default function PlantingRecordList() { return <AdminListPage title="Planting Records" tableName="planting_records" selectQuery="*, fields(field_name)" searchPlaceholder="Search..." searchFields={['crop_type','variety']} columns={[{label:'Crop',key:'crop_type',sortable:true,render:r=><span className="font-medium">{r.crop_type}</span>},{label:'Variety',key:'variety',render:r=>r.variety||'—'},{label:'Field',key:'field_id',render:r=>(r.fields as any)?.field_name||'—'},{label:'Planted',key:'planting_date',sortable:true,render:r=>new Date(r.planting_date).toLocaleDateString()},{label:'Area (ha)',key:'area_planted_hectares',render:r=>r.area_planted_hectares??'—'},{label:'Status',key:'status',render:r=><Badge variant="secondary">{r.status}</Badge>}]} />; }
