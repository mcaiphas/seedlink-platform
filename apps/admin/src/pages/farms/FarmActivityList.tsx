import { AdminListPage } from '@/components/AdminListPage';
import { Badge } from '@/components/ui/badge';
export default function FarmActivityList() { return <AdminListPage title="Farm Activities" tableName="farm_activities" selectQuery="*, farms(farm_name)" searchPlaceholder="Search activities..." searchFields={['title','activity_type']} orderBy="activity_date" columns={[{label:'Title',key:'title',sortable:true,render:r=><span className="font-medium">{r.title}</span>},{label:'Type',key:'activity_type',render:r=><Badge variant="secondary" className="capitalize">{r.activity_type}</Badge>},{label:'Farm',key:'farm_id',render:r=>(r.farms as any)?.farm_name||'—'},{label:'Date',key:'activity_date',sortable:true,render:r=>new Date(r.activity_date).toLocaleDateString()},{label:'Cost',key:'cost_amount',render:r=>r.cost_amount?`R ${r.cost_amount.toLocaleString()}`:'—'},{label:'Status',key:'status',render:r=><Badge variant={r.status==='completed'?'default':'outline'} className="capitalize">{r.status}</Badge>}]} />; }
