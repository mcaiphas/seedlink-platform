import { AdminListPage } from '@/components/AdminListPage';
import { Badge } from '@/components/ui/badge';
export default function HarvestRecordList() { return <AdminListPage title="Harvest Records" tableName="harvest_records" selectQuery="*, fields(field_name)" searchPlaceholder="Search..." searchFields={['quality_grade']} orderBy="harvest_date" columns={[{label:'Date',key:'harvest_date',sortable:true,render:r=>new Date(r.harvest_date).toLocaleDateString()},{label:'Field',key:'field_id',render:r=>(r.fields as any)?.field_name||'—'},{label:'Yield (tons)',key:'total_yield_tons',sortable:true,render:r=>r.total_yield_tons??'—'},{label:'Yield/ha',key:'yield_per_hectare',render:r=>r.yield_per_hectare??'—'},{label:'Quality',key:'quality_grade',render:r=>r.quality_grade?<Badge variant="outline">{r.quality_grade}</Badge>:'—'},{label:'Status',key:'status',render:r=><Badge variant="secondary">{r.status}</Badge>}]} />; }
